# Andalus Sweets - Project Structure

## 📁 البنية المقترحة (Simple & Organized)

```
/var/www/html/andalus_sweets/
│
├── index.html                 # الصفحة الرئيسية
├── food-restaurant.html       # صفحة المأكولات
├── sweets-restaurant.html     # صفحة الحلويات
├── special-orders.html        # صفحة العروض الخاصة
├── contact.html               # صفحة التواصل
├── orders.html                # صفحة السلة
├── checkout.html              # صفحة الدفع
├── login.html                 # صفحة تسجيل الدخول
│
├── assets/
│   ├── css/
│   │   ├── main.css           # CSS الرئيسي
│   │   ├── navbar.css         # CSS للشريط العلوي
│   │   ├── footer.css         # CSS للشريط السفلي
│   │   ├── home.css           # CSS للصفحة الرئيسية
│   │   ├── food.css           # CSS لصفحة المأكولات
│   │   ├── sweets.css         # CSS لصفحة الحلويات
│   │   ├── special-orders.css # CSS لصفحة العروض
│   │   └── responsive.css     # CSS للشاشات الصغيرة
│   │
│   ├── js/
│   │   ├── main.js            # JavaScript الرئيسي
│   │   ├── cart.js            # إدارة السلة
│   │   ├── navbar.js          # وظائف الشريط العلوي
│   │   └── utils.js           # وظائف مساعدة
│   │
│   ├── images/
│   │   ├── food/              # صور المأكولات
│   │   ├── sweets/             # صور الحلويات
│   │   ├── special_orders/    # صور العروض
│   │   ├── navbar/             # صور الشريط العلوي
│   │   ├── login/             # صور صفحة تسجيل الدخول
│   │   └── contact/           # صور صفحة التواصل
│   │
│   └── videos/
│       ├── food/              # فيديوهات المأكولات
│       ├── sweets/            # فيديوهات الحلويات
│       └── home/              # فيديوهات الصفحة الرئيسية
│
└── components/                # (اختياري) مكونات قابلة لإعادة الاستخدام
    ├── navbar.html
    └── footer.html
```

## 🎯 المميزات

1. **كل صفحة في ملف HTML منفصل** - سهل التعديل والصيانة
2. **CSS منفصل لكل صفحة** - سهل التعديل بدون تعقيد
3. **JavaScript بسيط** - بدون frameworks معقدة
4. **مجلدات منظمة** - كل شيء في مكانه الصحيح

## 📱 التوافق مع الموبايل (Mobile Responsive)

**نعم! سيكون متوافق 100% مع الموبايل** ✅

### المميزات:

1. **CSS Responsive** - نفس CSS الحالي مع media queries:

   - `@media (max-width: 480px)` - شاشات صغيرة
   - `@media (max-width: 768px)` - تابلت
   - `@media (max-width: 1024px)` - شاشات متوسطة

2. **Mobile Menu** - قائمة منسدلة للموبايل
3. **Touch-Friendly** - أزرار كبيرة سهلة اللمس
4. **Fast Loading** - ملفات أصغر = تحميل أسرع على الموبايل
5. **Viewport Meta Tag** - موجود في HTML:
   ```html
   <meta name="viewport" content="width=device-width, initial-scale=1.0" />
   ```

### مقارنة الأداء:

- **Vue.js الحالي**: 522KB JS (بطيء على الموبايل)
- **HTML بسيط**: ~50KB JS (أسرع بكثير على الموبايل)

**النتيجة**: HTML بسيط = أسرع وأفضل للموبايل! 🚀

## ⚠️ ملاحظة مهمة

**المشروع الحالي يستخدم Vue.js** - تحويله إلى HTML بسيط يتطلب:

- إعادة كتابة جميع الصفحات
- إعادة كتابة JavaScript بدون Vue
- إعادة كتابة CSS بدون scoped attributes
- إعادة كتابة نظام السلة والتنقل

**هل تريد:**

1. **تنظيم الملفات الحالية** فقط (بدون إعادة كتابة)؟
2. **تحويل كامل** من Vue.js إلى HTML بسيط (يتطلب إعادة كتابة)؟
